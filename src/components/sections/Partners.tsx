interface Partner {
  name: string;
  logo?: string;
  webLink?: string;
  active: boolean;
}

export function Partners() {
  // No partners data yet — renders nothing until populated
  return null;
}
